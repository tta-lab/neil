{
  "name": "neil",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "resume:build": "scripts/build-resume compile",
    "resume:watch": "scripts/build-resume watch",
    "deploy:prod": "bun run build && wrangler deploy --env prod"
  },
  "devDependencies": {
    "vite": "^8.0.16",
    "wrangler": "^4.100.0"
  },
  "dependencies": {
    "marked": "^18.0.5"
  }
}
